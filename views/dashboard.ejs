<%- include('partials/header'); -%>

<main class="body-pd" id="body-pd">
  <svg xmlns="http://www.w3.org/2000/svg" class="d-none">
    <symbol id="check-circle-fill" viewBox="0 0 16 16">
      <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zm-3.97-3.03a.75.75 0 0 0-1.08.022L7.477 9.417 5.384 7.323a.75.75 0 0 0-1.06 1.06L6.97 11.03a.75.75 0 0 0 1.079-.02l3.992-4.99a.75.75 0 0 0-.01-1.05z" />
    </symbol>
    <symbol id="info-fill" viewBox="0 0 16 16">
      <path
        d="M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16zm.93-9.412-1 4.705c-.07.34.029.533.304.533.194 0 .487-.07.686-.246l-.088.416c-.287.346-.92.598-1.465.598-.703 0-1.002-.422-.808-1.319l.738-3.468c.064-.293.006-.399-.287-.47l-.451-.081.082-.381 2.29-.287zM8 5.5a1 1 0 1 1 0-2 1 1 0 0 1 0 2z"
      />
    </symbol>
    <symbol id="exclamation-triangle-fill" viewBox="0 0 16 16">
      <path
        d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"
      />
    </symbol>
  </svg>
  <%- include('partials/sidebarMenu'); -%>

  <div class="d-flex flex-column flex-grow-1 bg-light">
    <div class="p-4">
      <div class="row">
        <div class="col">
          <h2>Assignment Dashboard</h2>
        </div>
        <% if(message.err){ %>

        <div class="alert alert-warning alert-dismissible fade show" role="alert">
          <svg class="bi flex-shrink-0 me-2" role="img" aria-label="Warning:" width="15px" height="15px">
            <use xlink:href="#exclamation-triangle-fill" />
          </svg>
          <%= message.err%>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <% }else if(message.succ){ %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <svg class="bi flex-shrink-0 me-2" role="img" aria-label="Success:" width="15px" height="15px">
            <use xlink:href="#check-circle-fill" />
          </svg>
          <%= message.succ%>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <% } %>
        <!-- Messages -->
        <div id="succ-not" class="alert alert-success alert-dismissible fade show d-none" role="alert">
          <svg class="bi flex-shrink-0 me-2" role="img" aria-label="Success:" width="15px" height="15px">
            <use xlink:href="#check-circle-fill" />
          </svg>
          <span id="succ-msg"></span>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>

        <div id="err-not" class="alert alert-warning alert-dismissible fade show d-none" role="alert">
          <svg class="bi flex-shrink-0 me-2" role="img" aria-label="Warning:" width="15px" height="15px">
            <use xlink:href="#exclamation-triangle-fill" />
          </svg>
          <span id="err-msg"></span>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>

        <!-- <div class="col">
          <button type="button" class="btn btn-sm btn-outline-primary" onclick="filterSelection('all')">
            All assignments
          </button>
          <button type="button" class="btn btn-sm btn-outline-primary" onclick="filterSelection('active')">
            Active
          </button>
          <button type="button" class="btn btn-sm btn-outline-primary" onclick="filterSelection('inactive')">
            Inactive
          </button>
        </div> -->
      </div>
      <% if(user.view != "asStudent"){ %>
      <div class="btn-toolbar mb-2 mb-md-0">
        <a type="button" href="/assignment/add" class="btn btn-sm btn-outline-secondary"> Add assignment </a>
      </div>
      <% } %>
      <div id="assignmentCard" class="row row-cols-1 row-cols-lg-4 align-items-stretch g-4 py-5">
        <% result.forEach(function(element){ %>
        <div class="col-xl-4 col-lg-6 col-md-6 col-sm-6 cardSingle">
          <div class="card h-100 overflow-hidden shadow-lg">
            <div class="row">
              <div class="col-1" style="background-color: <%=element[ 'Notebook.color'] %>"></div>
              <div class="col-2 d-flex justify-content-center align-items-center ps-4">
                <img alt="<%=element['Notebook.User.name']%>" src="<%=element['Notebook.User.pic']%>" class="rounded-circle img-responsive border border-dark" width="75" height="75" />
              </div>
              <div class="col-md-8 col-7">
                <div class="card-body d-flex flex-column justify-content-center pe-0">
                  <h5 class="card-title fs-4 fw-bolder"><%=element['Notebook.name']%></h5>
                  <p class="fw-normal mb-1"><%=element['Notebook.User.name']%></p>
                  <p class="fw-light text-body-secondary mb-0">Role: <%= element.role=='owner'? 'Teacher':'Student'; %></p>
                  <a href="/assignment/notebook-<%=element['Notebook.createdAt'].valueOf()%>-<%=element.notebook_id%>" class="stretched-link mt-auto submit-button"></a>
                </div>
              </div>
              <div class="col-md-1 col-2 pt-3 ps-0 pe-3 fs-5">
                <div class="dropdown">
                  <a class="bx bx-dots-vertical-rounded z-2 position-absolute" data-bs-toggle="dropdown" aria-expanded="false"></a>
                  <ul class="dropdown-menu position-fixed">
                    <% if(element.role=="owner" || element.role=="teacher"){ %>
                    <li><a class="dropdown-item" href="#">Copy invite link</a></li>
                    <li><a class="dropdown-item" href="#">Edit</a></li>
                    <li><a class="dropdown-item" href="#">Archive</a></li>
                    <% } %>
                    <% if(element.role=="student" || element.role=="teacher"){ %>
                    <li><a class="dropdown-item" href="#">Leave</a></li>
                    <% } %>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        </div>
        <% }); %>
      </div>
    </div>
  </div>
</main>

<script>
  const deleteModal = document.getElementById("deleteModal");
  if (deleteModal) {
    deleteModal.addEventListener("show.bs.modal", (event) => {
      // Button that triggered the modal
      const button = event.relatedTarget;
      // Extract info from data-bs-* attributes
      const ass_id = button.getAttribute("data-bs-assignmentid");

      // If necessary, you could initiate an Ajax request here
      // and then do the updating in a callback.

      // Update the modal's content.
      deleteModal.querySelector(".modal-footer button").onclick = () => {
        deleteAssignment(ass_id)
          .then(
            (response) => {
              document.getElementById(ass_id).remove();
              document.getElementById("succ-msg").innerHTML = response.msg;
              document.getElementById("succ-not").classList.remove("d-none");
            },
            (err) => {
              console.log(err);
              document.getElementById("err-msg").innerHTML = "There is some error!";
              document.getElementById("err-not").classList.remove("d-none");
            }
          )
          .catch((err) => {
            console.log(err);
            document.getElementById("err-msg").innerHTML = "There is some error!";
            document.getElementById("err-not").classList.remove("d-none");
          });
      };
    });
    async function deleteAssignment(recipient) {
      const response = await fetch("/assignment/delete/" + recipient);
      return await response.json();
    }
  }
</script>

<%- include('partials/footer'); -%>
